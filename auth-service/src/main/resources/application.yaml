server:
  port: 8082

secret:
  key: "secret123aswinsecret123aswinsecret123aswin"
  
spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/vendora_db
    username: root
    password: 2465
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/