spring:
  application:
    name: chat-service
  datasource:
    url: jdbc:mysql://localhost:3306/chat_db
    username: root
    password: 2465

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true